import { LeadsFilterPipe } from './leads-filter.pipe';

describe('LeadsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LeadsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
